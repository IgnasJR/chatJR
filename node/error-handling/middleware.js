// export const errorHandlingMiddleware = async (req, res, next) => {
//   if (error instanceof DomainError) {
//     res.status(400).json({ message: error.message });
//   } else {
//     res.status(500).json({ message: 'Internal server error' });
//   }

//   next();
// };
